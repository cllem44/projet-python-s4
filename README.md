# projet-python-s4 Esteban,Arthur,Clément
Voici la liste de toutes les librairies à installer:
py -m pip install customtkinter
py -m pip install Pillow
Volume PC : py -m pip install pycaw 
Volume PC : py -m pip install comtypes
py -m pip install vstools
Musique : py -m pip install pyglet (peut etre 2 fois)
Meteo : py -m pip install meteofrance_api
Horloge : py -m pip install DateTime
Horloge : py -m pip install tzdata
Parametre : py -m pip install screen-brightness-control
GPS : py -m pip install geocoder
GPS : py -m pip install tkintermapview
