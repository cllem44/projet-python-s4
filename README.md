# projet-python-s4
installer customtkinter dans l'invite de commande : py -m pip install customtkinter
installer Pillow dans l'invite de commande : py -m pip install Pillow (pour les fond d'ecrans et image en général)
taille ecran : "400x700"
